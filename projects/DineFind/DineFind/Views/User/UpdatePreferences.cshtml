@model List<DineFind.Models.Entities.Preference>
@{
    ViewBag.Title = "UpdatePreferences";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit Preferences</h2>

@using (Html.BeginForm("UpdatePreferences", "User", FormMethod.Post))
{
    for (int i = 0; i < Model.Count; i++)
    {
        <div>
            @*@Html.CheckBoxFor(m => Model[i].IsSelected)*@
            @*@Html.LabelFor(m => Model[i].Name)*@
        </div>
    }
    <input type="submit" value="Save Preferences" class="btn btn-primary" />
}
